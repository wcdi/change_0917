name: Testing debian(bookworm)

on:
  pull_request:
    branches: ["**"]
    types:
      - opened
      - synchronize

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: testing
        run: docker build -f ./tests/debian_bookworm/dockerfile .
