name: Tests

on:
  pull_request:
    branches: ["**"]
    types:
      - opened
      - synchronize

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ubuntu24.04
        run: docker build -f ./tests/ubuntu_2404/dockerfile .
