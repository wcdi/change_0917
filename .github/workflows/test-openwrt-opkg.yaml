name: Tests

on:
  pull_request:
    branches: ["**"]
    types:
      - opened
      - synchronize

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: openwrt_opkg
        run: docker build -f ./tests/openwrt_opkg/dockerfile .
